import 'package:flutter/material.dart';

class PenerbitPage extends StatelessWidget {
  const PenerbitPage({super.key});

  @override
  Widget build(BuildContext context) {

    return const Center(
      child: Text(
        "Penulis Page",
      ),
    );
  }
}